name: iam_s3

on:
  workflow_call:
    inputs:
      serviced_repos:
        required: true
        type: string
      slack_channel_ids:
        required: false
        type: string
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_REGION:
        required: true
      AWS_IAM_USER_NAME:
        required: true
      GH_ACCESS_TOKEN:
        required: true
      SLACK_BOT_TOKEN:
        required: false

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_IAM_USER_NAME: ${{ secrets.AWS_IAM_USER_NAME }}
  GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
  SERVICED_REPOS: ${{ inputs.serviced_repos }}
  SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
  SLACK_CHANNEL_IDS: ${{ inputs.slack_channel_ids }}

jobs:
  rotate_keys:
    runs-on: ubuntu-latest
    steps:
      - name: Checking-out remote scripts
        uses: actions/checkout@v3
        with:
          repository: oshopgiri/deploy_actions
          ref: aws_lambda_support
          path: ./
      - name: Setting-up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true
      - name: Rotating keys
        run: ruby rotate_iam_s3_keys.rb
